# -------- PostgreSQL (local Docker uses 5433 in my setup) --------
POSTGRES_HOST=localhost
POSTGRES_PORT=5433
POSTGRES_DB=dovah
POSTGRES_USER=dovah
POSTGRES_PASSWORD=dovah

# Preferred modern driver (psycopg v3):
DATABASE_URL=postgresql+psycopg://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
POSTGRES_URL=${DATABASE_URL}

# If you decide to use psycopg2 instead, swap the URL to:
# DATABASE_URL=postgresql+psycopg2://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# DB-less mode for Day-4 (skip Postgres altogether):
# DATABASE_URL=sqlite://

# -------- Python import path --------
PYTHONPATH=${PWD}/src

# -------- DOVAH tuning --------
DOVAH_SESSION_WINDOW=300
LATENCY_SLO_MS=2000

# -------- Dev-only pseudonymization keys --------
DOVAH_HMAC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
DOVAH_TENANT_SALT=dev_only_do_not_use

# ---- Python / SQLAlchemy URL (use psycopg v3 driver) ----
DATABASE_URL=postgresql+psycopg://dovah:dovah@localhost:5433/dovah

# ---- Legacy Python fallback (some of your code checks POSTGRES_URL) ----
POSTGRES_URL=postgresql+psycopg://dovah:dovah@localhost:5433/dovah

# ---- JDBC URL for Flink / Java connectors ----
JDBC_URL=jdbc:postgresql://localhost:5433/dovah
POSTGRES_USER=dovah
POSTGRES_PASSWORD=dovah

# ---- App settings ----
DOVAH_HMAC_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef
DOVAH_TENANT_SALT=dev_only_do_not_use
DOVAH_SCRUB_AWS=1
DOVAH_SESSION_WINDOW=300
LATENCY_SLO_MS=2000

# (Optional; only if you insist on running scripts by file path instead of -m)
# PYTHONPATH=./src 

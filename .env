# host port in your compose file is 5433
export DATABASE_URL="postgresql://dovah:dovah@localhost:5433/dovah"
# your code also checks POSTGRES_URL as a fallback:
export POSTGRES_URL="$DATABASE_URL"
(root / ".env").write_text("PYTHONPATH=${workspaceFolder}/src\n", encoding="utf-8")
